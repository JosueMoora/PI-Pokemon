const { Router } = require('express');
// Importar todos los routers;
// Ejemplo: const authRouter = require('./auth.js');
const pokemons = require('./Pokemons.js')
const type = requiere('./Types.js')

const router = Router();

// Configurar los routers



module.exports = router;
